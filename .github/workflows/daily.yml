name: Daily TIMEFLIP Run

on:
  schedule:
    # 00:30 UTC Monday–Saturday
    - cron: '30 23 * * 1-6'
  workflow_dispatch:

jobs:
  run-timeflip:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          
      - name: Run TIMEFLIP Engine
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python trainer.py
          
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: timeflip-results
          path: |
            timeflip_*.json
            timeflip_breakdown_*.json
